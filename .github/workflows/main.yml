name: IITM Action Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f2002146@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Second Step
        run: echo "GitHub Action Triggered Successfully"
